@model IEnumerable<Contact>

@{
    ViewData["Title"] = "التعليقات";
}

<!-- Content wrapper -->
<!-- Custom style1 Breadcrumb -->
<div class="content-wrapper">
    <!-- Content -->

    <div class="container-xxl flex-grow-1 container-p-y">


        <!-- Users List Table -->
        <div class="card">

            <div class="row gx-3 gy-2 align-items-center mt-1 mb-4 ms-1 me-1">
                <div class="table-responsive text-nowrap">


                    <table class="table TProject">
                        <thead>
                            <tr>
                                <th>الرقم</th>


                                <th>
                                   الأسم

                                </th>

                                <th>الإيميل</th>
                                <th>الموضوع</th>
                                <th>الرسالة</th>
                            </tr>
                        </thead>
                        <tbody class="table-border-bottom-0">
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>@item.Id</td>
                                    <td> @item.Name</td>
                                    <td>@item.Email</td>
                                    <td>@item.Subject</td>
                                    <td>@item.Message</td>
                                    @* <th>
                                        <div class="dropdown">
                                            <button type="button" class="btn dropdown-toggle hide-arrow p-0" data-bs-toggle="dropdown"
                                                    aria-expanded="false">
                                                <i class="bx bx-dots-vertical-rounded"></i>
                                            </button>
                                            <div class="dropdown-menu">

                                                <a asp-action="AddOrEdit" asp-route-id="@item.Id" class="dropdown-item">
                                                    <i class="bx bxs-edit-alt icon-padding edit-color"></i>

                                                    تعديل
                                                </a>
                                                <a asp-action="AddOrEdit" asp-route-id="@item.Id" class="dropdown-item" id="confirmText">
                                                    <i class="bx bxs-trash-alt icon-padding delet-color" aria-hidden="true"></i>
                                                    حذف
                                                </a>





                                            </div>
                                        </div>
                                    </th> *@



                                </tr>
                            }


                        </tbody>
                    </table>
                </div>
            </div>


        </div>
    </div>
</div>


